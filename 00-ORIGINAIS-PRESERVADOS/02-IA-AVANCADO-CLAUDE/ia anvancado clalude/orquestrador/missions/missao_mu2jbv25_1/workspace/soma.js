// Declaração do array de números
const numeros = [3, 5, 7];

// Cálculo da soma usando reduce
const total = numeros.reduce((acc, val) => acc + val, 0);

// Exibe o resultado
console.log('Soma total:', total);
