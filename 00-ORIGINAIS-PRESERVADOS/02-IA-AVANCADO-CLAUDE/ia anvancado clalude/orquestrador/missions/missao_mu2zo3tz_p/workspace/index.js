/*\n * Soma dois números com validação de tipo rigorosa.\n * @param {number} a - Primeiro valor numérico.\n * @param {number} b - Segundo valor numérico.\n * @returns {number} Resultado da soma de a e b.\n * @throws {TypeError} Se algum argumento não for do tipo Number.\n */\nfunction soma(a, b) {\n  if (typeof a !== 'number' || typeof b !== 'number') {\n    throw new TypeError('Ambos os argumentos devem ser do tipo Number.');\n  }\n  return a + b;\n}\n// Execução de exemplo\ntry {\n  console.log(soma(5, 3));\n} catch (erro) {\n  console.error(erro.message);\n}