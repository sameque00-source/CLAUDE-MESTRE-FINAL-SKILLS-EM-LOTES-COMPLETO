# Observability and Reliability

Use structured logs, metrics, traces, health checks, and alerts where they materially improve diagnosis.

For remote/external calls use bounded timeouts, retry only safe operations, use idempotency where needed, and fail gracefully.

For incidents: contain impact, reproduce safely, isolate root cause, recover, verify, then document corrective actions and prevention.
